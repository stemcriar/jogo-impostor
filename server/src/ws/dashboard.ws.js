// The dashboard specific handlers can go here if needed.
// Most logic is handled by join_game and sync:request in handler.js
export function handleDashboardEvents(io, socket) {
  // Available for future dashboard-specific events
}
